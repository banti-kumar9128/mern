services:
  - type: web
    name: mern-backend
    runtime: node
    startCommand: npm start
    buildCommand: npm install
    buildDir: backend
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 4000
    healthCheckPath: /health
    healthCheckInterval: 30
    autoDeploy: true
